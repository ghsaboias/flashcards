{
  "name": "flashcards",
  "version": "0.4.3",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute flashcards-db --file=src/db/schema.sql --remote",
    "db:init:local": "wrangler d1 execute flashcards-db --file=src/db/schema.sql --local",
    "fetch:countries": "tsx scripts/fetch-countries.ts",
    "seed": "tsx scripts/seed.ts --remote",
    "seed:local": "tsx scripts/seed.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.61.0"
  }
}
